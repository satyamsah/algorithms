class DinoSpeed{

    class Dino implements comparable<Dino>{
        String name;
        int speed;
        
        int comapator(Dino d){
         return this.speed-d.speed;
        
        }
        String toString(){
            return name;
        }
        
    }

public static void main(String [] str){

    Sting inputspecies="trex";
    Map<String, Int> map= new HashMap<String, speed>();
    for (line :file2){
        if(lines[2]=="trex"){
        map.put(line[0],line[1]);
        }
    }
    for(line:file1){
        if(map.contains(line[0]){
        string leglength=map.get(line[0]);
        tring hinglength=line[1]
        map.put(line[0], hinglength*)
        }
    }
    
    List<Dino> arr= new ArrayList<>();
    for (Map.Entry<String, int> singlemap , hmap){
     
     arr.add(new Dino(singlemap.key, singlmap,value);
     
     Array.sort(arr);
     
     for(int i=0;i<5;i++){
     res.add(arr[i].name);
     }
     
     return res;
    }
    
    
    
}
}